---
applyTo: "client/src/**/*.{ts,tsx}"
---

# Frontend Instructions

- Keep route UX explicit (loading, error, not-found behaviors).
- Favor shared utilities/components over one-off logic.
- Maintain strict type safety and path alias usage (`@/`).
- Validate build/lint after UI or route-level changes.
